// Centralized service configuration
export const SERVICE_CONFIG = {} as const;

export const DEFAULT_MODELS = {
    OPENAI_EMBEDDING: 'text-embedding-3-small',
    OPENAI_LLM: 'gpt-4o-mini',
} as const;
